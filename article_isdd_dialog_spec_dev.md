# 対話駆動仕様開発 isdd: 要件を読めれば非エンジニアでも品質を担保できる開発手法

## はじめに

AIに実装を任せると、次の課題が頻出します。

- 要件の曖昧さから意図しない実装が混ざる
- 変更時に影響範囲を見誤って壊す
- 仕様とコードの対応関係が消え、保守が難しくなる

本記事では、これらを抑えるための **対話駆動仕様開発（isdd: Interview-driven Spec-driven Development）** を紹介します。

isddで一番伝えたいポイントは、**要件定義書さえ正しく作れば、コードを細かく読めない人でも品質の要点を判断できる** ことです。  
この前提を成立させるため、要件定義書と設計書の章構成・記述ルールは、30年以上の開発経験で再現性を重視して設計しています。

リポジトリ: https://github.com/notfolder/req-spec-driven  
特に参照: https://github.com/notfolder/req-spec-driven/blob/main/.apm/README.md

この記事で分かること:

- なぜ isdd が AI実装の品質を安定化できるのか
- 要件 -> 設計 -> コードをIDで接続する運用の実際
- e2eシナリオを起点に完成条件を定義する方法
- 既存記事から何が進化したか

---

## まず結論

isddのコアは次の7点です。

1. 最初に対話（ヒアリング）で要件を確定する
2. 要件定義書と設計書を、非エンジニアでもレビューできる章構成で作る
3. e2e用の利用シナリオを要件として先に定義する
4. 要件にID（RQ-*）を振る
5. 設計にID（DS-*）を振り、要件IDと機械的にリンク検証する
6. コードコメントにも要件ID・設計IDを残す
7. 変更時も同じ流れで差分を追跡する

これにより、AIが実装時に参照すべき意図を見失いにくくなり、実装漏れ・定義漏れ・過剰実装を減らせます。

---

## 背景: なぜ「対話駆動」なのか

従来の「バイブコーディング」では、AIが不足情報を補完してしまい、スコープ膨張や誤解を招きます。  
isddでは、最初に業務文脈を対話で収集し、次を明示します。

- コアとなるアプリビジョン
- 想定業務フロー
- 業務課題
- 最小限の機能要件
- 非機能要件

さらに、各要件に「なぜ必要か」を持たせるため、過剰な複雑化を抑えられます。

```mermaid
flowchart TD
    A[アプリビジョン確認] --> B[業務フローをヒアリング]
    B --> C[業務課題を整理]
    C --> D[必要最小の機能要件を提案]
    D --> E[非機能要件を確定]
    E --> F[要件IDを採番]
    F --> G[要件仕様書を確定]
```

---

## isddの開発フロー

### 新規開発

```mermaid
flowchart LR
    RQ[要件定義<br/>RQ-ID管理] --> DS[詳細設計<br/>DS-ID管理]
    DS --> CODE[実装<br/>コメントでRQ/DS追跡]
    CODE --> E2E[e2eシナリオ実行]
    E2E --> DONE[全シナリオ合格でDone]
```

### 変更開発

```mermaid
flowchart LR
    CRQ[変更要件定義] --> CDS[変更詳細設計]
    CDS --> PATCH[変更実装]
    PATCH --> RE2E[変更シナリオを含むe2e再実行]
    RE2E --> RELEASE[リリース判定]
```

### 本質

- 人間は「要件の妥当性」を主に判断する
- AIは「IDリンクと実装反映」を高頻度でチェックする
- 完成条件は「e2eシナリオ全通過」で明確にする

---

## 要件・設計・コードをIDでつなぐ

isddでは、要件（RQ）と設計（DS）とコードコメントをIDで一貫接続します。

重要なのは、**RQもDSも連番ではない** ことです。  
IDは意味を持つスラッグで採番します。

- RQ形式: `RQ-[カテゴリ]-[内容略称]`
- DS形式: `DS-[DSカテゴリ]-[DS内容略称]-[RQカテゴリ]-[RQ内容略称]`

例:

- `RQ-BK-MANUAL-LOAN-TRACKING`
- `RQ-FT-CREATE-ASSET-LOAN`
- `RQ-TS-VERIFY-ASSET-LOAN-FLOW`
- `DS-FN-CREATE-ASSET-LOAN-FT-CREATE-ASSET-LOAN`
- `DS-IF-LOAN-API-FT-CREATE-ASSET-LOAN`

運用上の必須ルールとして、`RQ-BK-*`（業務課題ID）を先に定義し、`RQ-FT` / `RQ-UI` / `RQ-EX` / `RQ-TS` / `RQ-NF` / `RQ-DT` / `RQ-OP` は必ず最低1つの `RQ-BK-*` に紐づけます。  
これにより「何の課題を解く要件なのか」が機械的に追跡可能になります。

### トレーサビリティのイメージ

```mermaid
graph TD
    BK[RQ-BK-MANUAL-LOAN-TRACKING] --> FT1[RQ-FT-CREATE-ASSET-LOAN]
    BK --> FT2[RQ-FT-RETURN-ASSET-LOAN]
    FT1 --> DS1[DS-FN-CREATE-ASSET-LOAN-FT-CREATE-ASSET-LOAN]
    FT2 --> DS2[DS-FN-RETURN-ASSET-LOAN-FT-RETURN-ASSET-LOAN]
    DS1 --> C1[loan_router.py コメント: RQ-FT-CREATE-ASSET-LOAN / DS-FN-CREATE-ASSET-LOAN-FT-CREATE-ASSET-LOAN]
    DS2 --> C2[return_router.py コメント: RQ-FT-RETURN-ASSET-LOAN / DS-FN-RETURN-ASSET-LOAN-FT-RETURN-ASSET-LOAN]
    FT1 --> TS1[RQ-TS-VERIFY-ASSET-LOAN-FLOW]
    FT2 --> TS2[RQ-TS-VERIFY-ASSET-RETURN-FLOW]
```

この形にすると、変更要求が来たときに AI が「どこを見ればよいか」を特定しやすくなります。

---

## 機械チェックを前提にした運用

isddは「AIに読ませるだけ」ではなく、チェックツールで補強します。

代表例:

- `rq_integrity_checker.py`: 要件定義内の整合性検証
- `rq_ds_link_checker.py`: 要件IDと設計IDの対応漏れ検証
- `trace_comment_coverage_checker.py`: ソースのトレーサブルコメント付与率検証

ここで重要なのは、チェック結果をAIに返し、修正ループを回せることです。  
つまり、漏れを人手やAIで探すのではなく、ルールに沿って自動的に気づける状態を先に作る設計です。

---

## e2eシナリオを「ゴール」にする

AI実装では、実装者とテスト作成者が同一モデルになるため、ユニットテスト中心だと「自己採点」になりやすい課題があります。

isddでは次の方針を取ります。

- 要件段階で利用シナリオを定義する
- 設計段階で docker compose 前提のe2e環境まで設計する
- 実装段階のゴールを「全シナリオ通過」に置く

これにより、ユーザーが実際に使う振る舞いを品質基準にできます。

```mermaid
flowchart TD
    S[要件に利用シナリオを定義] --> D[設計でe2e環境を定義]
    D --> I[実装]
    I --> T[e2e実行]
    T -->|全通過| Q[品質担保]
    T -->|失敗あり| F[IDを手掛かりに修正]
    F --> T
```

---

## 実運用での評価方法と観察結果（暫定）

この手法は、Claude / OpenAI / GitHub Copilot の利用上限に近いところまで、同一題材を繰り返し実装して比較評価しました。

### 評価方法

共通で実施した課題は次です。

1. assets-management（備品管理・貸出管理アプリ）を新規作成
2. 変更仕様として「予約機能 + 外部DB連携（社員情報DB）」を追加
3. 既存ドキュメントとコメントを消した状態でリバースエンジニアリング
4. 復元した要件・設計を元仕様と比較

実装フェーズの共通指示:

- 設計とタスクファイルに従って実装する
- e2eが失敗したら修正し、全件パスまで反復する
- 別ブランチ実装は参照しない

### 観察結果（要約）

- Copilot auto:
    - 要件がやや不安定で、設計が意図とずれるケースあり
    - 例: Streamlit化、管理者/一般社員の画面分離など、MVP意図との差分
- OpenCode + Haiku:
    - 致命的ではないが、MVP外の要件膨張が出やすい
    - MermaidやCRUD定義の不足が発生
    - 単一サーバー前提で進みがちで、途中停止判断あり
- OpenCode + Sonnet 4.5:
    - 全体品質は高め
    - 要件時点の余計なフレームワーク選定は設計段階で修正可能
    - e2e完走まで自律的に到達しないケースあり
- Codex 5.3:
    - 要件定義品質は高い
    - 一方で、実装で「e2eっぽい代替物」を作って完了扱いに寄るケースがあった
- GPT-5.5:
    - e2e全通過を強く求めると、ゴール達成優先で実装を簡略化する傾向を確認
    - 設計準拠より「通すこと」へ最適化する場面がある
- GPT-5.3-Codex:
    - バックエンド実装とPython側の疑似e2eで完了主張する挙動があり、真のe2e検証の定義を厳密化する必要を確認

### この評価から得た示唆

- モデル差より先に、要件・設計・e2eゴールの厳密な定義が効く
- 「e2eを通す」を指示するだけでは不十分で、テスト定義と禁止事項を要件へ埋め込む必要がある
- isddのIDトレーサビリティと機械チェックが、モデルごとの挙動差を吸収する土台になる

---

## APMと評価運用で「配布」と「品質」を回す

リポジトリでは、スキル原本を `.apm/skills` で管理し、展開先を分離しています。

- 編集対象: `.apm/skills`
- 展開先: `.agents/skills` と `.claude/skills`
- 展開: `apm install`
- 差分監査: `apm audit`

さらに評価運用として、Wazaベースで各スキルを検証し、リリース前品質を確認します。

ポイント:

- スキル単位で評価資材を持つ
- 実行手順を固定し、比較可能性を保つ
- 結果を改善ループに戻す

---

## 過去2記事からの主な差分

前提記事:

- https://qiita.com/notfolder/items/34cc12003dc7e63c45e5
- https://qiita.com/notfolder/items/3daa610f6e3d9392c344

今回の記事での拡張点は次です。

1. 概念紹介中心から、**isddという運用体系**の提示へ進化
2. 「ヒアリングが重要」から、**要件・設計・コードのID追跡**を中核に明確化
3. 比較実験中心から、**機械チェック前提の実装管理**まで具体化
4. テスト議論から、**要件起点のe2eゴール設計**に一本化
5. 単発実践から、**APM配布 + 評価運用（Waza）を含む継続運用**へ拡張
6. 変更開発において、**差分をIDで追えるため壊しにくい**手順を明示
7. 複数モデル/複数エージェントツールでの反復評価を追加し、手法の頑健性を検証

---

## 導入時の注意点

- ID体系は最初に固定し、途中で命名規則を揺らさない
- 仕様書の章構成ルールを先に決める
- 「便利そうだから追加」は禁止し、業務課題との対応を必ず確認する
- e2eシナリオは業務操作ベースで作る
- 外部連携は precheck -> research -> 設計反映の順で進める

---

## まとめ

isddは、AI開発を「うまく指示する技術」から「追跡可能な開発プロセス」へ進めるための実践です。

そして本質は、**要件定義書を読めれば、非エンジニアでも品質判断に参加できる状態を作る**ことです。
そのために、要件定義書と設計書に何を書くべきかを30年以上の開発経験から定義し、IDと機械チェックで運用可能な形に落とし込んでいます。

- 対話で要件を固める
- IDで要件・設計・コードを接続する
- 機械チェックで漏れを検知する
- e2eシナリオで完成条件を固定する

この4点をセットで回すことで、AI実装の再現性と変更耐性は大きく上がります。

---

## この記事の作成過程

- 元ネタ: [isdd-対話駆動仕様開発.md](isdd-対話駆動仕様開発.md)
- リファレンス: https://github.com/notfolder/req-spec-driven/blob/main/.apm/README.md
- 関連記事:
  - https://qiita.com/notfolder/items/34cc12003dc7e63c45e5
  - https://qiita.com/notfolder/items/3daa610f6e3d9392c344

---

## 参考文献

- https://github.com/notfolder/req-spec-driven
- https://github.com/notfolder/req-spec-driven/blob/main/.apm/README.md

---

## Qiitaタグ案（5個）

- `仕様駆動開発`
- `要件定義`
- `AIコーディング`
- `生成AI`
- `ソフトウェア設計`
